services:
  - type: worker
    name: sagkol-cahuu-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "bash start.sh"
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
